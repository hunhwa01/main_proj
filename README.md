npm install

npm audit

npm start

npm install @supabase/supabase-js

conda activate venv

uvicorn backend.main:app --reload

npm install react-google-places-autocomplete

pip install python-dotenv

pip install httpx

참고해야 할 점: Walk2.jsx 프론트 추가 해야하는데
안 될 수도 있음 난 무서워서 시도 못했어 먄 ㅎ;
